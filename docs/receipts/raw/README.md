# Raw receipt data

These files are the command output and trial data behind the dated receipt
summaries one directory above. Developer-specific absolute paths were
normalized to `<repo>/` or `~/` before publication; measurements, hashes,
timestamps, model revisions, and hardware metadata are unchanged.
